<p id="notice"><%= notice %></p>

<% if @page.render %>
	<%= render @page.render %>
<% else %>
	<%= markdown @page.content %>
<% end %>

<%= link_to "Edit", edit_page_path(@page) %> |
<%= link_to "Back", pages_path %>
