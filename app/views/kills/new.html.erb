<h1>New Kill</h1>

<p>
	Congratulations on killing <%= @target.name %>!
	The kill will need to be confirmed by <%= @target.name %> or an admin before it counts.
	Please fill out the information below about your kill.
	It is optional, but it is greatly appreciated if it is filled out.
</p>

<%= form_for(@kill, url: kills_create_path) do |f| %>
	<div class="form-group">
		<%= f.label :picture_url, "URL of picture of kill" %>
		<br>
		<%= f.text_field :picture_url, class: "form-control" %>
	</div>

	<div class="form-group">
		<%= f.label :how, "How did this kill occur?" %>
		<br>
		<%= f.text_area :how, class: "form-control" %>
	</div>

	<%= f.hidden_field :participation_id, value: default_participation_id(@kind, @target.team.participation.game.id) %>
	<%= f.hidden_field :target_id, value: @target.id %>
	<%= f.hidden_field :kind, value: @kind if @kind %>

	<div class="actions">
		<%= f.submit class: "btn btn-success" %>
	</div>
<% end %>
