<h1>New Neutralization</h1>

<p>
	Sorry about being neutralized!
	It will need to be confirmed by <%= @neutralizer.name %> or an admin before it becomes official, though.
	Please fill out the information below about the neutralization.
	It is optional, but it is greatly appreciated if it is filled out.
</p>

<%= form_for(@neutralization) do |f| %>
	<div class="form-group">
		<%= f.label :picture_url, "URL of picture of kill" %>
		<br>
		<%= f.text_field :picture_url, class: "form-control" %>
	</div>

	<div class="form-group">
		<%= f.label :how, "How did this kill occur?" %>
		<br>
		<%= f.text_area :how, class: "form-control" %>
	</div>

	<%= f.hidden_field :killer_id, value: current_user.id %>
	<%= f.hidden_field :target_id, value: @neutralizer.id %>
	<%= f.hidden_field :game_id, value: @neutralizer.team.participation.game_id %>

	<div class="actions">
		<%= f.submit class: "btn btn-success" %>
	</div>
<% end %>
